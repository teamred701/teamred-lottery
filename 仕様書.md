# 抽選会アプリ 仕様書

**対象コミュニティ:** TEAM RED  
**作成日:** 2026-06-14

---

## 1. 概要

TEAM RED をはじめとする複数イベントの抽選を Web ブラウザ上で実施するためのアプリ。  
重み付き抽選・Discord 通知・Google スプレッドシート記録・Notion 記録を一体化し、  
本番運用に必要な「辞退再抽選」「リハーサルモード」「共有 URL」も備える。

### ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体の UI |
| `app.js` | ロジック全般（状態管理・抽選・外部連携） |
| `gas_fukugyo_spreadsheet.gs` | Google Apps Script（スプレッドシート / Notion プロキシ） |

---

## 2. アプリ状態（グローバル）

`appState` オブジェクトがアプリ全体の状態を保持する。

| プロパティ | 型 | 説明 |
|---|---|---|
| `participants` | `Map<string, number>` | 参加者名 → 口数 |
| `winners` | `Set<string>` | 当選歴のある参加者名 |
| `winnerHistory` | `Array<Record>` | 当選履歴詳細（イベント・賞・日時含む） |
| `excludedParticipants` | `Set<string>` | 手動除外された参加者名 |
| `allowDuplicateWinners` | `boolean` | 重複当選許可フラグ（既定: `false`） |
| `isLotteryRunning` | `boolean` | 抽選アニメーション中フラグ |
| `lastResults` | `Array<{prize, name}>` | 直前の抽選結果（取り消し用） |
| `selectedTime` | `number` | アニメーション秒数（3/5/10/15/30/60） |
| `spreadsheetEnabled` | `boolean` | スプレッドシート送信 ON/OFF |
| `spreadsheetUrl` | `string` | GAS WebアプリURL |
| `discordEnabled` | `boolean` | Discord 通知 ON/OFF |
| `discordWebhookUrl` | `string` | Discord Webhook URL |
| `notionToken` | `string` | Notion Integration Token |
| `notionDatabaseId` | `string` | Notion データベース ID |
| `rehearsalMode` | `boolean` | リハーサルモードフラグ |

`currentRound` は再抽選ラウンド内の状態（イベント名・賞名・辞退者）を保持する。

---

## 3. 機能一覧

### 3-1. 参加者管理

#### 個別追加
- 名前（必須）と口数（1以上の整数、既定: 1）を入力して追加
- Enter キーでもボタンと同じ動作
- 同名の参加者が既に存在する場合、口数を**加算**（上書きではない）

#### 一括追加
- テキストエリアに 1 行 1 名で入力
- 書式: `名前[,または空白][口数]`（口数省略時は 1）
- 例: `佐藤花子,3` / `山田次郎 5` / `鈴木一郎`
- 同名の場合も加算

#### 編集・削除
- `编集` ボタン: prompt で口数を変更
- `削除` ボタン: confirm 後に削除

#### 全員 1 口に統一
- 全参加者の口数を 1 に一括変更

#### CSV エクスポート
- 列構成: `名前, 口数`（UTF-8 BOM 付き）
- ファイル名: `参加者リスト_YYYYMMDD.csv`

#### CSV インポート
- 1 行目が `名前` / `name` で始まる場合はヘッダーとしてスキップ
- 既存参加者がいる場合、追加または上書きを選択できる

### 3-2. 抽選設定

| 項目 | 選択肢・制約 |
|---|---|
| イベント名 | ドロップダウン（9 種 + 空白）|
| 月 | 1〜12 月（CNPRED など「static」イベントは月非表示） |
| 賞品名 | ドロップダウン 22 種（1等〜9等、リアタイ賞、NFT 各種、各種枠、RWA など） |
| 当選者数 | 1 以上かつ利用可能人数以下（重複当選 ON 時は上限なし。参加者数を超える指定が可能） |
| 除外対象者 | テキストエリア、1 行 1 名 |
| 抽選時間 | 3/5/10/15/30/60 秒 |
| 重複当選設定 | OFF（1人1回まで） / ON（何度でも可） |
| Discord 通知 | トグル |
| リハーサルモード | トグル |

#### イベント名の生成ルール
- 通常: `{イベント名} {月}月抽選会`（例: `TEAM RED 6月抽選会`）
- static イベント（CNPRED など）: イベント名がそのままイベント名になる（月フィールド非表示）

### 3-3. 抽選アルゴリズム

```
1. 参加者プールを構築
   - excludedParticipants に含まれる人を除外
   - allowDuplicateWinners が false の場合、winners（当選歴）に含まれる人を除外

2. 当選者数ぶん繰り返す:
   a. 総口数を計算
   b. window.crypto.getRandomValues（なければ Math.random）で乱数生成
   c. 重み付き選択（累積口数 >= 乱数×総口数 の最初の人が当選）
   d. allowDuplicateWinners が false の場合、当選者をプールから除去（同回内の重複防止）。
      true の場合はプールに残す（同回内で同じ人が複数回当選できる）
   e. allowDuplicateWinners が false かつ rehearsalMode が false の場合、winners に追加
```

乱数生成には **暗号学的に安全な `crypto.getRandomValues`** を優先して使用する。

### 3-4. 辞退・再抽選

1. 結果画面の各当選者に「辞退・再抽選」ボタンを表示
2. 辞退者を `currentRound.declinedNames` に追加
3. `winners` から除去（重複 OFF 時、次回抽選で対象に戻す）
4. `winnerHistory` から最新一致レコードを削除
5. 現在の当選者（他の人）+ 辞退者を除いたプールで 1 名を再抽選
6. 新当選者を `winnerHistory` に追加（rehearsalMode 時はスキップ）
7. Discord・Notion への通知も再送（rehearsalMode 時はスキップ）

### 3-5. 抽選取り消し

- 「この抽選を取り消す」ボタン押下
- `lastResults` 全員を `winners` / `winnerHistory` から削除
- 結果カードを非表示にする
- Discord・Notion への送信済み通知は**取り消せない**

### 3-6. 外部連携

#### Discord 通知
- Webhook URL に POST（Embed 形式）
- タイトル: `🎊 {賞名} 当選者発表 🎊`
- 本文: イベント名 + 当選者番号付きリスト

#### Google スプレッドシート
- GAS WebアプリURL に `mode: 'no-cors'` で POST
- ボディ: `{ winners: [{name, prize, eventName, displayTime, timestamp}] }`

#### Notion
- GAS プロキシ経由で Notion API に POST
- 記録プロパティ: 名前（イベント名）、抽選日時、賞名、当選者名、口数、総参加者数、総口数

#### リハーサルモード時
上記 3 連携はすべてスキップ。当選履歴への記録もスキップ。

### 3-7. 共有 URL

1. `winnerHistory` を JSON にシリアライズ → UTF-8 → Base64 (URL-safe) でエンコード
2. `?share=<data>` のクエリパラメータ付き URL を生成
3. クリップボードにコピーし、フィードバックを 2 秒間ボタンに表示
4. URL からの読み込み時は閲覧専用で当選履歴を表示し、URL パラメータをクリア

### 3-8. データ永続化（localStorage）

保存キー: `lottery-app-data`

保存タイミング: 参加者の追加/編集/削除、設定変更、抽選実行後など、状態が変わるたび

保存内容: 参加者リスト・当選歴・当選履歴詳細・除外対象者・フォーム値・各種設定

### 3-9. リハーサルモード

| 状態 | 挙動 |
|---|---|
| OFF → ON | ページ上部にオレンジバナーを表示 |
| 抽選実行 | 当選者発表タイトルに「【リハーサル】」を付加 |
| 外部連携 | Discord / スプレッドシート / Notion への送信をすべてスキップ |
| 当選履歴 | `winners` と `winnerHistory` を更新しない |
| 設定保存 | localStorage に保存されるため、ページ再読込後も持続 |

### 3-10. その他 UI

- **リアルタイム時計**: 1 秒ごとに現在時刻を表示（`MM/DD HH:mm:ss`）
- **抽選ボタン有効化条件**: イベント名・月が選択済み、参加者 1 名以上、当選者数 ≤ 利用可能人数、抽選実行中でない
- **除外状態表示**: 当選歴による除外人数・手動除外人数をカード内にリアルタイム表示（最大 4 名 + 他○名）

---

## 4. 対応イベント

| イベント名 | 月フィールド |
|---|---|
| TEAM RED | 必要 |
| SPORTS of HEART | 必要 |
| 赤犬赤猫 | 必要 |
| ゆるトークン | 必要 |
| セブDAOの日 | 必要 |
| Scent Japan DAO | 必要 |
| CNPRED | 不要（static） |
| LEVELUP | 必要 |
| 複業アカデミー | 必要 |

---

## 5. 賞の優先度（ソート用）

| 賞品名 | 優先度 |
|---|---|
| 1等〜9等 | 1〜9 |
| リアタイ賞 | 10 |
| CNP | 11 |
| CNPRED | 12 |
| カケラNFT / KAKERANFT | 13 |
| Premium特典枠 | 14 |
| モバイル枠 | 15 |
| CNP REDホルダー枠 | 16 |
| CNP REDコンプリート枠 | 17 |
| 応援ギフト枠 | 18 |
| RWA | 19 |
| もみじとかえでのカケラ🎁 | 20 |
| 抽選 | 21 |
| その他 | 22 |
| 未定義 | 99 |

当選履歴の表示・クリップボードコピー時に使用。同一時刻の複数抽選は優先度順（昇順）で表示。

---

## 6. 外部サービス設定値

> 本番環境の設定値は `app.js` の `appState` 初期値として埋め込まれている。

| サービス | 用途 |
|---|---|
| Google Apps Script（スプレッドシート） | 当選データ送信 |
| Google Apps Script（Notion プロキシ） | Notion API へのリレー |
| Discord Webhook | 当選結果通知 |
| Notion Database | 当選記録の永続化 |

---

## 7. セキュリティ考慮事項

- Discord Webhook URL・Notion Token が `app.js` にハードコードされている
- GAS プロキシ経由の Notion 書き込みは `mode: 'no-cors'` のため成否をクライアントで確認できない
- 共有 URL のデータはクライアントサイド Base64 エンコードのみで暗号化なし

---

## 8. ブラウザ要件

- `window.crypto.getRandomValues` サポート（モダンブラウザ全般）
- `navigator.clipboard.writeText`（HTTPS または localhost 必須）
- `localStorage` 利用可能
- JavaScript 有効
